#include <stdio.h>

/**
 *
 * main - main function to print the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main ()
{
	int ch;
	for(ch = 'A'; ch <= 'Z'; ch++)
		putchar(ch);
	return (0);
}
