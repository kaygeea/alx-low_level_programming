#include "main.h"

/**
 * print_alphabet - prints the alphabet, in lowercase
 *
 * Return: Always 0 (Success)
 */
int print_alphabet(void)
{
	char b = 'a';

	while (c <= 'z')
	{
		_putchar(b);
		b++;
	}
	_putchar('\n');
	return (0);
}
